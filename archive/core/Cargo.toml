[package]
name = "culinary-core"
version = "1.0.0"
description = "Shared game logic for Culinary Alchemy native clients"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
