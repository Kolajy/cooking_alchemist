# Godot Visual Parity Pass 6

## Goal
Fix invisible locked toolbar buttons, workspace hint text, active method highlighting, scroll sheet depth.

## Changes
- `technique_tools.gd` — `get_active_method()` for toolbar active state
- `workspace.gd` — hint text, enabled locked buttons, active method fix, focus_mode
- `cozy_theme.gd` — readable locked chips, parchment scroll frame
- `main.gd` — dark hearth margin around scroll sheet

## Validation
- Headless load exit 0

## Status
Completed.
