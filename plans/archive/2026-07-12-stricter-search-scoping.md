# Archived Plan - Stricter Search Scoping in AGENTS.md

Update the search scoping rule in `AGENTS.md` to explicitly specify NEVER searching legacy or archived platforms (`archive/`) unless requested.

## Execution Steps
1. Update `AGENTS.md` with the stricter scoping rule.
2. Archive the plan and update `plans/README.md`.

## Result
- Modified `- **Search Scoping**: Do NOT search...` to `- **Search Scoping**: NEVER search, grep, or read source files or directories in legacy or archived platforms (`archive/`) unless explicitly specified by the user.` inside `AGENTS.md`.
