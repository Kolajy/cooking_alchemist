# Archived Plan - Resolve PWA sw.js Conflict in Branch feat-cabinet-search-suggestions-15271044867407367762

Checkout the suggestions feature branch, merge `main` into it, and resolve the add/add conflict in `web/src/dev-dist/sw.js`.

## Execution Steps
1. Checkout `feat-cabinet-search-suggestions-15271044867407367762`.
2. Merge `main` into it.
3. Resolve the conflict in `web/src/dev-dist/sw.js` by selecting the main revision hash for `index.html`.
4. Update `web/package.json` on the branch to use the dynamic test runner.
5. Commit and push the resolved merge.
6. Return to `main`.
7. Archive the plan.

## Result
- Merge completed and pushed. Conflict in `web/src/dev-dist/sw.js` resolved.
- Updated branch to use the dynamic test runner.
